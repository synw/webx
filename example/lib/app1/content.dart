import 'package:flutter/material.dart';

class App1Content extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("App1 content"),
    );
  }
}
